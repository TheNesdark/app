plugins {
    id 'java'
}

repositories {
    mavenCentral()
    google()
}

sourceSets {
    main {
        java {
            srcDirs = []
        }
        resources {
            srcDirs = ['src/main/resources']
        }
    }
}

tasks.named('compileJava') {
    enabled = false
}

dependencies {
    // some dependencies
}
