package com.genoma.plus.dao.impl.comprabs;

import com.genoma.plus.dao.comprabs.ConsolidadoContratosDAO;
import com.genoma.plus.dto.comprabs.ConsolidadoContratosDTO;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;
import javax.sql.DataSource;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;

/* JADX INFO: loaded from: GenomaP.jar:com/genoma/plus/dao/impl/comprabs/ConsolidadoContratosDAOImpl.class */
public class ConsolidadoContratosDAOImpl implements ConsolidadoContratosDAO {
    private JdbcTemplate jdbcTemplate;

    public void setDataSource(DataSource dataSource) {
        this.jdbcTemplate = new JdbcTemplate(dataSource);
    }

    @Override // com.genoma.plus.dao.comprabs.ConsolidadoContratosDAO
    public List<ConsolidadoContratosDTO> listar(int i, boolean xFiltro, String xcheck) {
        String sql;
        if (xFiltro) {
            sql = "SELECT `cc_orden_compra`.`Id`\n, `cc_orden_compra`.`NInterno`\n, `cc_orden_compra`.`FechaC`\n, `cc_terceros`.`RazonSocialCompleta` AS `Tercero`\n, `cc_unidad_funcional`.`Nbre` AS `UnidadF`\n, `cc_tipo_documentof`.`Nbre` AS `TipoDoc`\n, `cc_orden_compra`.`ObjetivoG`\n, `cc_orden_compra`.`ValorP`\n, `cc_orden_compra`.`Iva`\n, CONCAT(CONCAT(g_persona.Apellido1 ,' ', g_persona.Apellido2 ,' ', g_persona.Nombre1 ,' ', g_persona.Nombre2)) AS `Administrador`\n, `rh_tipo_cargo`.`Nbre` AS `cargo`\n, cc_orden_compra.IdDisponibilidad \n, IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`) FechaFin \n, cc_orden_compra.Estado\n, IF(ROUND(DATEDIFF(cc_orden_compra.`FechaFin`, cc_orden_compra.`FechaC`)/30)<=0,  CONCAT(1,' Mes'),CONCAT(ROUND(DATEDIFF(cc_orden_compra.`FechaFin`, cc_orden_compra.`FechaC`)/30), ' Meses')) AS Duracion \n, cc_orden_compra.EsProg \n, IF(cc_orden_compra_cierre.`Estado`=1,-2,\n\t\t       IF( `cc_tipo_documentof`.`NDias`=0, -1,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=30), 0,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>30 \n  AND DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=90), 1,\n  IF(DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>90, 2,3))))) Validacion \n , IFNULL(s.`Seguimiento`,'') Seguimientos\n , IFNULL(d.`Documentos`,'') Documentos\n , IFNULL(a.`Adicion`,'') Adicion\n , SUM(IFNULL(v.PVigencia,'0')) PVigencia\n , '' TipoGasto\nFROM `cc_unidad_funcional`\nINNER JOIN `cc_orden_compra`  ON (`cc_unidad_funcional`.`Id` = `cc_orden_compra`.`Id_UnidadF`)\nINNER JOIN `cc_tipo_documentof` ON (`cc_orden_compra`.`Id_TipoDocF` = `cc_tipo_documentof`.`Id`)\nINNER JOIN `rh_tipo_persona_cargon` ON (`rh_tipo_persona_cargon`.`Id` = `cc_orden_compra`.`Id_Administrador`)\nINNER JOIN `cc_terceros`  ON (`cc_terceros`.`Id` = `cc_orden_compra`.`Id_Tercero`)\nINNER JOIN `rh_unidadf_cargo`  ON (`rh_tipo_persona_cargon`.`Id_unidadf_cargo` = `rh_unidadf_cargo`.`Id`)\nINNER JOIN `g_persona` ON (`rh_tipo_persona_cargon`.`Id_Persona` = `g_persona`.`Id`)\nINNER JOIN `rh_tipo_cargo`  ON (`rh_unidadf_cargo`.`Id_Cargo` = `rh_tipo_cargo`.`Id`)\n LEFT JOIN `cc_orden_compra_adicion` \n        ON (`cc_orden_compra_adicion`.`Id_OrdenCompra` = `cc_orden_compra`.`Id`) AND (cc_orden_compra_adicion.`Estado`=1)  \nLEFT  JOIN `cc_orden_compra_cierre` ON (`cc_orden_compra_cierre`.`Id_Orden_Compra` = `cc_orden_compra`.`Id`) AND (cc_orden_compra_cierre.`Estado`=1)\nLEFT JOIN (SELECT\n   `Id_OrdenC`\n   , GROUP_CONCAT(CONCAT(`FechaS`, ' ',`Observacion`)ORDER BY FechaS SEPARATOR '\\n' ) Seguimiento \n   ,Estado   \nFROM\n   `cc_orden_compra_seguimiento`\n   GROUP BY `Id_OrdenC`\n) s ON  (s.`Id_OrdenC` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (s.Estado=1)\n\nLEFT JOIN (SELECT\n   `cc_orden_compra_documento`.`Id_OrdenCompra`\n   , GROUP_CONCAT(`cc_tipo_documentos_soporte`.`Nbre` ORDER BY Nbre SEPARATOR '\\n' ) AS Documentos\n   , `cc_orden_compra_documento`.`Estado`\nFROM\n   `cc_orden_compra_documento`\n   INNER JOIN `cc_tipo_documentos_soporte` \n       ON (`cc_orden_compra_documento`.`Id_TipoDocumento` = `cc_tipo_documentos_soporte`.`Id`)\n       GROUP BY `Id_OrdenCompra`\n) d ON  (d.`Id_OrdenCompra` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (d.Estado=1)\n    LEFT JOIN (SELECT `cc_orden_compra_adicion`.`Id_OrdenCompra`\n     , GROUP_CONCAT(CONCAT('DOCUMENTO: ',`cc_tipo_documentof`.`Nbre`,\n     ' - ',IF(`cc_orden_compra_adicion`.`Tipo`=0,CONCAT('FECHA ',IFNULL(`cc_orden_compra_adicion`.`FechaP`,'')),CONCAT('VALOR ',IFNULL(`cc_orden_compra_adicion`.`Valor`,'')))) ORDER BY Nbre SEPARATOR '\\n' ) AS Adicion\n     , `cc_orden_compra_adicion`.`Estado`\nFROM\n     `cc_orden_compra_adicion`\n      INNER JOIN `cc_tipo_documentof` \n          ON (`cc_orden_compra_adicion`.`Id_TipoDocF` = `cc_tipo_documentof`.`Id`)\n          GROUP BY `Id_OrdenCompra`\n) a ON  (a.`Id_OrdenCompra` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (a.Estado=1)\nLEFT JOIN (SELECT cc_radicacion.Id_Proveedor,\n    cc_radicacion.Estado,\n    cc_radicacion.`NoCompra`,\n    (cc_radicacion.`SubTotal`+cc_radicacion.`Otros`+cc_radicacion.`Flete`+cc_radicacion.`Iva`) PVigencia\n    FROM cc_radicacion \n    LEFT JOIN `pp_movimiento`ON (`pp_movimiento`.`Id` = `cc_radicacion`.`Id_Compromiso`) \n    INNER JOIN cc_terceros  ON (cc_radicacion.Id_Proveedor = `cc_terceros`.Id) \n    INNER JOIN cc_conceptocmsa  ON (cc_radicacion.Id_Concepto= cc_conceptocmsa.Id)   \n    ORDER BY cc_radicacion.Id  DESC ) v ON (v.`Id_Proveedor` = `cc_terceros`.`Id`) AND (v.NoCompra = `cc_orden_compra`.`NInterno`) AND (v.Estado=1)\n WHERE (`cc_orden_compra`.`Estado` =1 AND DATE_FORMAT(cc_orden_compra.`FechaC`, '%Y')='" + i + "' AND cc_tipo_documentof.Tipo=3 AND IF(cc_orden_compra_cierre.`Estado`=1,-2,\n  IF( `cc_tipo_documentof`.`NDias`=0, -1,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=30), 0,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>30 \n  AND DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=90), 1,\n  IF(DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>90, 2,3))))) IN (" + xcheck + "))\n GROUP BY `cc_orden_compra`.`Id` ORDER BY cc_orden_compra.`FechaC` DESC";
        } else {
            sql = "SELECT `cc_orden_compra`.`Id`\n, `cc_orden_compra`.`NInterno`\n, `cc_orden_compra`.`FechaC`\n, `cc_terceros`.`RazonSocialCompleta` AS `Tercero`\n, `cc_unidad_funcional`.`Nbre` AS `UnidadF`\n, `cc_tipo_documentof`.`Nbre` AS `TipoDoc`\n, `cc_orden_compra`.`ObjetivoG`\n, `cc_orden_compra`.`ValorP`\n, `cc_orden_compra`.`Iva`\n, CONCAT(CONCAT(g_persona.Apellido1 ,' ', g_persona.Apellido2 ,' ', g_persona.Nombre1 ,' ', g_persona.Nombre2)) AS `Administrador`\n, `rh_tipo_cargo`.`Nbre` AS `cargo`\n, cc_orden_compra.IdDisponibilidad \n, IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`) FechaFin \n, cc_orden_compra.Estado\n, IF(ROUND(DATEDIFF(cc_orden_compra.`FechaFin`, cc_orden_compra.`FechaC`)/30)<=0,  CONCAT(1,' Mes'),CONCAT(ROUND(DATEDIFF(cc_orden_compra.`FechaFin`, cc_orden_compra.`FechaC`)/30), ' Meses')) AS Duracion \n, cc_orden_compra.EsProg \n, IF(cc_orden_compra_cierre.`Estado`=1,-2,\n\t\t       IF( `cc_tipo_documentof`.`NDias`=0, -1,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=30), 0,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>30 \n  AND DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=90), 1,\n  IF(DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>90, 2,3))))) Validacion \n , IFNULL(s.`Seguimiento`,'') Seguimientos\n , IFNULL(d.`Documentos`,'') Documentos\n , IFNULL(a.`Adicion`,'') Adicion\n , SUM(IFNULL(v.PVigencia,'0')) PVigencia\n , '' TipoGasto\nFROM `cc_unidad_funcional`\nINNER JOIN `cc_orden_compra`  ON (`cc_unidad_funcional`.`Id` = `cc_orden_compra`.`Id_UnidadF`)\nINNER JOIN `cc_tipo_documentof` ON (`cc_orden_compra`.`Id_TipoDocF` = `cc_tipo_documentof`.`Id`)\nINNER JOIN `rh_tipo_persona_cargon` ON (`rh_tipo_persona_cargon`.`Id` = `cc_orden_compra`.`Id_Administrador`)\nINNER JOIN `cc_terceros`  ON (`cc_terceros`.`Id` = `cc_orden_compra`.`Id_Tercero`)\nINNER JOIN `rh_unidadf_cargo`  ON (`rh_tipo_persona_cargon`.`Id_unidadf_cargo` = `rh_unidadf_cargo`.`Id`)\nINNER JOIN `g_persona` ON (`rh_tipo_persona_cargon`.`Id_Persona` = `g_persona`.`Id`)\nINNER JOIN `rh_tipo_cargo`  ON (`rh_unidadf_cargo`.`Id_Cargo` = `rh_tipo_cargo`.`Id`)\n LEFT JOIN `cc_orden_compra_adicion` \n        ON (`cc_orden_compra_adicion`.`Id_OrdenCompra` = `cc_orden_compra`.`Id`) AND (cc_orden_compra_adicion.`Estado`=1)  \nLEFT  JOIN `cc_orden_compra_cierre` ON (`cc_orden_compra_cierre`.`Id_Orden_Compra` = `cc_orden_compra`.`Id`) AND (cc_orden_compra_cierre.`Estado`=1)\nLEFT JOIN (SELECT\n   `Id_OrdenC`\n   , GROUP_CONCAT(CONCAT(`FechaS`, ' ',`Observacion`)ORDER BY FechaS SEPARATOR '\\n' ) Seguimiento \n   ,Estado   \nFROM\n   `cc_orden_compra_seguimiento`\n   GROUP BY `Id_OrdenC`\n) s ON  (s.`Id_OrdenC` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (s.Estado=1)\n\nLEFT JOIN (SELECT\n   `cc_orden_compra_documento`.`Id_OrdenCompra`\n   , GROUP_CONCAT(`cc_tipo_documentos_soporte`.`Nbre` ORDER BY Nbre SEPARATOR '\\n' ) AS Documentos\n   , `cc_orden_compra_documento`.`Estado`\nFROM\n   `cc_orden_compra_documento`\n   INNER JOIN `cc_tipo_documentos_soporte` \n       ON (`cc_orden_compra_documento`.`Id_TipoDocumento` = `cc_tipo_documentos_soporte`.`Id`)\n       GROUP BY `Id_OrdenCompra`\n) d ON  (d.`Id_OrdenCompra` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (d.Estado=1)\n    LEFT JOIN (SELECT `cc_orden_compra_adicion`.`Id_OrdenCompra`\n     , GROUP_CONCAT(CONCAT('DOCUMENTO: ',`cc_tipo_documentof`.`Nbre`,\n     ' - ',IF(`cc_orden_compra_adicion`.`Tipo`=0,CONCAT('FECHA ',IFNULL(`cc_orden_compra_adicion`.`FechaP`,'')),CONCAT('VALOR ',IFNULL(`cc_orden_compra_adicion`.`Valor`,'')))) ORDER BY Nbre SEPARATOR '\\n' ) AS Adicion\n     , `cc_orden_compra_adicion`.`Estado`\nFROM\n     `cc_orden_compra_adicion`\n      INNER JOIN `cc_tipo_documentof` \n          ON (`cc_orden_compra_adicion`.`Id_TipoDocF` = `cc_tipo_documentof`.`Id`)\n          GROUP BY `Id_OrdenCompra`\n) a ON  (a.`Id_OrdenCompra` =IFNULL(`cc_orden_compra`.`Id`,0)) AND (a.Estado=1)\nLEFT JOIN (SELECT cc_radicacion.Id_Proveedor,\n    cc_radicacion.Estado,\n    cc_radicacion.`NoCompra`,\n    (cc_radicacion.`SubTotal`+cc_radicacion.`Otros`+cc_radicacion.`Flete`+cc_radicacion.`Iva`) PVigencia\n    FROM cc_radicacion \n    LEFT JOIN `pp_movimiento`ON (`pp_movimiento`.`Id` = `cc_radicacion`.`Id_Compromiso`) \n    INNER JOIN cc_terceros  ON (cc_radicacion.Id_Proveedor = `cc_terceros`.Id) \n    INNER JOIN cc_conceptocmsa  ON (cc_radicacion.Id_Concepto= cc_conceptocmsa.Id)   \n    ORDER BY cc_radicacion.Id  DESC ) v ON (v.`Id_Proveedor` = `cc_terceros`.`Id`) AND (v.NoCompra = `cc_orden_compra`.`NInterno`) AND (v.Estado=1)\nWHERE (`cc_orden_compra`.`Estado` =1  AND cc_tipo_documentof.Tipo=3 AND IF(cc_orden_compra_cierre.`Estado`=1,-2,\n  IF( `cc_tipo_documentof`.`NDias`=0, -1,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=30), 0,\n  IF( (DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>30 \n  AND DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())<=90), 1,\n  IF(DATEDIFF(ADDDATE(IF(`cc_orden_compra_adicion`.`FechaP` IS NULL, cc_orden_compra.`FechaFin`, `cc_orden_compra_adicion`.`FechaP`), INTERVAL `cc_tipo_documentof`.`NDias` DAY), NOW())>90, 2,3))))) IN (" + xcheck + "))\n GROUP BY `cc_orden_compra`.`Id` ORDER BY cc_orden_compra.`FechaC` DESC";
        }
        RowMapper rowMapper = new RowMapper() { // from class: com.genoma.plus.dao.impl.comprabs.ConsolidadoContratosDAOImpl.1
            public Object mapRow(ResultSet rs, int i2) throws SQLException {
                ConsolidadoContratosDTO contrato = new ConsolidadoContratosDTO();
                contrato.setId(rs.getInt("Id"));
                contrato.setNInterno(rs.getString("NInterno"));
                contrato.setFechaC(rs.getString("FechaC"));
                contrato.setTercero(rs.getString("Tercero"));
                contrato.setUnidadF(rs.getString("UnidadF"));
                contrato.setTipoDocumento(rs.getString("TipoDoc"));
                contrato.setObjetivoG(rs.getString("ObjetivoG"));
                contrato.setValorP(Double.valueOf(rs.getDouble("ValorP")));
                contrato.setIva(Double.valueOf(rs.getDouble("Iva")));
                contrato.setAdministrador(rs.getString("Administrador"));
                contrato.setCargo(rs.getString("cargo"));
                contrato.setIdDisponibilidad(rs.getInt("IdDisponibilidad"));
                contrato.setFechaFinC(rs.getString("FechaFin"));
                contrato.setEstado(rs.getInt("Estado"));
                contrato.setDuracion(rs.getString("Duracion"));
                contrato.setEsProg(rs.getInt("EsProg"));
                contrato.setValidacion(rs.getInt("Validacion"));
                contrato.setSeguimientos(rs.getString("Seguimientos"));
                contrato.setDocumentos(rs.getString("Documentos"));
                contrato.setAdicion(rs.getString("Adicion"));
                contrato.setPVigencia(Double.valueOf(rs.getDouble("PVigencia")));
                contrato.setTipoGasto(rs.getString("TipoGasto"));
                return contrato;
            }
        };
        List<ConsolidadoContratosDTO> lsTipo = this.jdbcTemplate.query(sql, rowMapper);
        return lsTipo;
    }
}
