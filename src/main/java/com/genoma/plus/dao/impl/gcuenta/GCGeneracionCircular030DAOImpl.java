package com.genoma.plus.dao.impl.gcuenta;

import com.genoma.plus.dto.gcuenta.GCGenericoDTO;
import com.genoma.plus.jpa.gcuenta.GCGeneracionCircular030DAO;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;
import javax.sql.DataSource;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;

/* JADX INFO: loaded from: GenomaP.jar:com/genoma/plus/dao/impl/gcuenta/GCGeneracionCircular030DAOImpl.class */
public class GCGeneracionCircular030DAOImpl implements GCGeneracionCircular030DAO {
    private JdbcTemplate jdbcTemplate;

    public void setDataSource(DataSource dataSource) {
        this.jdbcTemplate = new JdbcTemplate(dataSource);
    }

    public List<Object[]> listFacturasCircular030(String xFechaI, String xFechaF, String xidEmpresa, int opc) {
        String sql;
        RowMapper mapper = (rs, rowNum) -> {
            Object[] fila = {Long.valueOf(rs.getLong("Tipo")), Long.valueOf(rs.getLong("CS")), rs.getString("Id_TipoIdentificacion"), rs.getString("No_identificacion"), rs.getString("NEmpresa"), rs.getString("TipoIdNormal"), Long.valueOf(rs.getLong("NIEmpresa")), rs.getString("Forma"), rs.getString("Prefijo"), Long.valueOf(rs.getLong("No_FacturaEvento")), rs.getString("Indicador"), Long.valueOf(rs.getLong("TotalEps")), rs.getString("Fecha_FacturaEvento"), rs.getString("FechaEntrega"), rs.getString("FechaDevolucion"), Long.valueOf(rs.getLong("VPagado")), Long.valueOf(rs.getLong("VAceptado")), rs.getString("Glosa"), Long.valueOf(rs.getLong("Total")), rs.getString("CJuridico"), rs.getString("IdEtapaProcedo")};
            return fila;
        };
        if (opc == 0) {
            sql = "SELECT 2 Tipo ,@numeroConsecutivo:=@numeroConsecutivo+1 AS `CS`, \n`cc_tercero`.`Id_TipoIdentificacion`,\n`cc_tercero`.`No_identificacion`,\n`cc_tercero`.`NEmpresa`,\nTipoIdNormal,\n`d_ips`.`NIEmpresa`,\n'F' Forma ,\n`f_factura_evento`.`Prefijo` ,\n`f_factura_evento`.`No_FacturaEvento_M` No_FacturaEvento,\n`f_factura_evento`.`Indicador`,\n`f_liquidacion`.`TotalEps`,\n`f_factura_evento`.`Fecha_FacturaEvento`,\n`f_factura_evento`.`FechaEntrega`,\n'' FechaDevolucion\n,'' PrefijoRcobro ,'' FRecobroRcobro,\np.VPagado ,\ng.VAceptado,\nIF(g.VAceptado=0,'NO','SI') Glosa,\n(`f_liquidacion`.`TotalEps` - (p.VPagado +g.VAceptado)) Total,\nIF(`f_factura_evento`.`CJuridico`=0,'NO','SI')CJuridico ,\n`f_factura_evento`.`IdEtapaProcedo` \nFROM  `d_ips`, `f_factura_evento` \nINNER JOIN  `f_liquidacion`  ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \nINNER JOIN  `f_empresacontxconvenio`  ON (`f_empresacontxconvenio`.`Id` = `f_liquidacion`.`IdEmpresaContxConv`) \nINNER JOIN  `g_empresacont`  ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \nINNER JOIN  `cc_tercero`  ON (`cc_tercero`.`Id` = `g_empresacont`.`Id_empresa`) \nLEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorA`,0))) AS `VAceptado` \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorR`,0))) AS `VRecuperable` \n    FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`) \n     LEFT JOIN  `f_factura_glosa`  \n         ON (`f_factura_glosa`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_glosa.`Estado`=1) AND (f_factura_glosa.`FechaG`<='" + xFechaF + "') \n     LEFT JOIN  `f_factura_glosa_respuesta`  \n         ON (`f_factura_glosa_respuesta`.`Id_Factura_glosa` = `f_factura_glosa`.`Id`) AND (f_factura_glosa_respuesta.`Estado`=1)  AND (DATE_FORMAT(f_factura_glosa_respuesta.`Fecha`, '%Y-%m-%d')<='" + xFechaF + "')  \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n GROUP BY `f_liquidacion`.`Id`) g ON (f_liquidacion.`Id`=g.IdLiquidacion) \n LEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_pagos`.`Valor`,0))) AS `VPagado`    \n      \n FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`)     \n     LEFT JOIN  `f_factura_pagos`  \n         ON (`f_factura_pagos`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_pagos.`Estado`=1) AND (f_factura_pagos.`FechaAbono`<='" + xFechaF + "') \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n      \n GROUP BY `f_liquidacion`.`Id`) p ON (f_liquidacion.`Id`=p.IdLiquidacion)  \n\n\n, (SELECT @numeroConsecutivo:=0) r \n WHERE ( `g_empresacont`.Id_TipoEmprCont IN(1,2) AND `f_factura_evento`.`FechaReporte` >='" + xFechaI + "' AND `f_factura_evento`.`FechaReporte` <='" + xFechaF + "' \nAND `f_liquidacion`.`TotalEps` <>0 AND `f_factura_evento`.`Estado` =0 \nAND `f_factura_evento`.`EstaArmada` =2) ORDER BY CS";
        } else {
            sql = "SELECT 2 Tipo ,@numeroConsecutivo:=@numeroConsecutivo+1 AS `CS`, \n`cc_tercero`.`Id_TipoIdentificacion`,\n`cc_tercero`.`No_identificacion`,\n`cc_tercero`.`NEmpresa`,\nTipoIdNormal,\n`d_ips`.`NIEmpresa`,\n'F' Forma ,\n`f_factura_evento`.`Prefijo` ,\n`f_factura_evento`.`No_FacturaEvento_M` No_FacturaEvento,\n`f_factura_evento`.`Indicador`,\n`f_liquidacion`.`TotalEps`,\n`f_factura_evento`.`Fecha_FacturaEvento`,\n`f_factura_evento`.`FechaEntrega`,\n'' FechaDevolucion\n,'' PrefijoRcobro ,'' FRecobroRcobro,\np.VPagado ,\ng.VAceptado,\nIF(g.VAceptado=0,'NO','SI') Glosa,\n(`f_liquidacion`.`TotalEps` - (p.VPagado +g.VAceptado)) Total,\nIF(`f_factura_evento`.`CJuridico`=0,'NO','SI')CJuridico ,\n`f_factura_evento`.`IdEtapaProcedo` \nFROM  `d_ips`, `f_factura_evento` \nINNER JOIN  `f_liquidacion`  ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \nINNER JOIN  `f_empresacontxconvenio`  ON (`f_empresacontxconvenio`.`Id` = `f_liquidacion`.`IdEmpresaContxConv`) \nINNER JOIN  `g_empresacont`  ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \nINNER JOIN  `cc_tercero`  ON (`cc_tercero`.`Id` = `g_empresacont`.`Id_empresa`) \nLEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorA`,0))) AS `VAceptado` \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorR`,0))) AS `VRecuperable` \n    FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`) \n     LEFT JOIN  `f_factura_glosa`  \n         ON (`f_factura_glosa`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_glosa.`Estado`=1) AND (f_factura_glosa.`FechaG`<='" + xFechaF + "') \n     LEFT JOIN  `f_factura_glosa_respuesta`  \n         ON (`f_factura_glosa_respuesta`.`Id_Factura_glosa` = `f_factura_glosa`.`Id`) AND (f_factura_glosa_respuesta.`Estado`=1)  AND (DATE_FORMAT(f_factura_glosa_respuesta.`Fecha`, '%Y-%m-%d')<='" + xFechaF + "')  \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n GROUP BY `f_liquidacion`.`Id`) g ON (f_liquidacion.`Id`=g.IdLiquidacion) \n LEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_pagos`.`Valor`,0))) AS `VPagado`    \n      \n FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`)     \n     LEFT JOIN  `f_factura_pagos`  \n         ON (`f_factura_pagos`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_pagos.`Estado`=1) AND (f_factura_pagos.`FechaAbono`<='" + xFechaF + "') \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n      \n GROUP BY `f_liquidacion`.`Id`) p ON (f_liquidacion.`Id`=p.IdLiquidacion)  \n\n\n, (SELECT @numeroConsecutivo:=0) r \nWHERE ( `g_empresacont`.Id_TipoEmprCont IN(1,2) AND `f_factura_evento`.`FechaReporte` >='" + xFechaI + "' AND `f_factura_evento`.`FechaReporte` <='" + xFechaF + "' \nAND `cc_tercero`.`Id`='" + xidEmpresa + "' AND `f_liquidacion`.`TotalEps` <>0 AND `f_factura_evento`.`Estado` =0 \nAND `f_factura_evento`.`EstaArmada` =2) ORDER BY CS";
        }
        List<Object[]> list = this.jdbcTemplate.query(sql, mapper);
        return list;
    }

    public List<GCGenericoDTO> listadoEmpresa030(String xFechaI, String xFechaF) {
        String sql = "SELECT `cc_tercero`.`Id`,\n`cc_tercero`.`NEmpresa`\nFROM  `d_ips`, `f_factura_evento` \nINNER JOIN  `f_liquidacion`  ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \nINNER JOIN  `f_empresacontxconvenio`  ON (`f_empresacontxconvenio`.`Id` = `f_liquidacion`.`IdEmpresaContxConv`) \nINNER JOIN  `g_empresacont`  ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \nINNER JOIN  `cc_tercero`  ON (`cc_tercero`.`Id` = `g_empresacont`.`Id_empresa`) \nLEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorA`,0))) AS `VAceptado` \n     , ROUND(SUM(IFNULL(`f_factura_glosa_respuesta`.`ValorR`,0))) AS `VRecuperable` \n    FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`) \n     LEFT JOIN  `f_factura_glosa`  \n         ON (`f_factura_glosa`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_glosa.`Estado`=1) AND (f_factura_glosa.`FechaG`<='" + xFechaF + "') \n     LEFT JOIN  `f_factura_glosa_respuesta`  \n         ON (`f_factura_glosa_respuesta`.`Id_Factura_glosa` = `f_factura_glosa`.`Id`) AND (f_factura_glosa_respuesta.`Estado`=1)  AND (DATE_FORMAT(f_factura_glosa_respuesta.`Fecha`, '%Y-%m-%d')<='" + xFechaF + "')  \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n GROUP BY `f_liquidacion`.`Id`) g ON (f_liquidacion.`Id`=g.IdLiquidacion) \n LEFT JOIN (SELECT \n      `f_liquidacion`.`Id` AS `IdLiquidacion`       \n     , ROUND(SUM(IFNULL(`f_factura_pagos`.`Valor`,0))) AS `VPagado`    \n      \n FROM \n      `f_factura_evento` \n     INNER JOIN  `f_liquidacion`  \n         ON (`f_factura_evento`.`Id_Liquidacion` = `f_liquidacion`.`Id`) \n     INNER JOIN  `f_empresacontxconvenio`  \n         ON (`f_liquidacion`.`IdEmpresaContxConv` = `f_empresacontxconvenio`.`Id`) \n     INNER JOIN  `g_empresacont`  \n         ON (`f_empresacontxconvenio`.`Id_EmpresaCont` = `g_empresacont`.`Id_empresa`) \n     INNER JOIN  `f_tiporegimen`  \n         ON (`f_empresacontxconvenio`.`Id_TipoRegimen` = `f_tiporegimen`.`Id`) \n     INNER JOIN  `cc_terceros`  \n         ON (`cc_terceros`.`Id` = `g_empresacont`.`Id_empresa`)     \n     LEFT JOIN  `f_factura_pagos`  \n         ON (`f_factura_pagos`.`Id_Liquidacion` = `f_liquidacion`.`Id`) AND (f_factura_pagos.`Estado`=1) AND (f_factura_pagos.`FechaAbono`<='" + xFechaF + "') \n WHERE (f_factura_evento.`Fecha_FacturaEvento` <='" + xFechaF + "'\n     AND `f_liquidacion`.`Estado` =0 \n     AND `f_factura_evento`.`Estado` =0 \n     AND `f_factura_evento`.`EstaArmada` =2) \n      \n GROUP BY `f_liquidacion`.`Id`) p ON (f_liquidacion.`Id`=p.IdLiquidacion)  \n\n\n, (SELECT @numeroConsecutivo:=0) r \nWHERE ( `g_empresacont`.Id_TipoEmprCont IN(1,2) AND `f_factura_evento`.`FechaReporte` >='" + xFechaI + "' AND `f_factura_evento`.`FechaReporte` <='" + xFechaF + "' \nAND `f_liquidacion`.`TotalEps` <>0 AND `f_factura_evento`.`Estado` =0 \nAND `f_factura_evento`.`EstaArmada` =2) GROUP BY `cc_tercero`.`Id` ORDER BY `cc_tercero`.`NEmpresa` ASC";
        RowMapper mapper = new RowMapper() { // from class: com.genoma.plus.dao.impl.gcuenta.GCGeneracionCircular030DAOImpl.1
            public Object mapRow(ResultSet rs, int rowNum) throws SQLException {
                GCGenericoDTO g = new GCGenericoDTO();
                g.setId(rs.getInt("Id"));
                g.setNombre(rs.getString("NEmpresa"));
                return g;
            }
        };
        List<GCGenericoDTO> list = this.jdbcTemplate.query(sql, mapper);
        return list;
    }
}
